# WEB322 Assignment 3 - Task Manager

## Overview
Express.js app using MongoDB (Mongoose) for user authentication and PostgreSQL (Sequelize) for tasks. Uses bcrypt and client-sessions.

## Setup
1. Copy `.env.example` → `.env` and fill values. URL-encode MongoDB password if needed.
2. Install dependencies:
