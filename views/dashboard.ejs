<h2>Dashboard</h2>

<p>Welcome, <strong><%= user ? user.username : 'Guest' %></strong></p>

<div class="stats">
  <div>Total tasks: <strong><%= total %></strong></div>
  <div>Completed: <strong><%= completed %></strong></div>
  <div>Pending: <strong><%= pending %></strong></div>
</div>

<p><a href="/tasks">View Tasks</a> | <a href="/tasks/add">Add Task</a></p>
